services:
  ai-llama:
    build: .
    ports:
      - "7777:7777"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - my-network

networks:
  my-network:
    driver: bridge